{
  "name": "MT4Account",
  "type": "object",
  "properties": {
    "account_name": {
      "type": "string",
      "description": "Friendly name for this MT4 account"
    },
    "login_id": {
      "type": "string",
      "description": "MT4 login ID"
    },
    "server_name": {
      "type": "string",
      "description": "MT4 server name"
    },
    "broker_name": {
      "type": "string",
      "description": "Broker name"
    },
    "account_balance": {
      "type": "number",
      "description": "Current account balance"
    },
    "account_equity": {
      "type": "number",
      "description": "Current account equity"
    },
    "is_active": {
      "type": "boolean",
      "default": true,
      "description": "Whether this account is currently active"
    },
    "last_connected": {
      "type": "string",
      "format": "date-time",
      "description": "Last successful connection timestamp"
    }
  },
  "required": [
    "account_name",
    "login_id",
    "server_name"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "write": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}